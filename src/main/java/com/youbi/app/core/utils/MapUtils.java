package com.youbi.app.core.utils;

import org.springframework.stereotype.Component;

@Component
public class MapUtils {
//
//    public List<MapMarker> getLocation(List<Intersection> intersections){
//        List<MapMarker> results = new ArrayList<>();
//        for(int i = 0; i < intersections.size(); i++){
//            Intersection intersection = intersections.get(i);
//            MapMarker a = new MapMarker(intersection.getIntersectionId(), 1,
//                    intersection.getIntersectionType().getIntersectionTypeId(), intersection.getIntersectionLong(),
//                    intersection.getIntersectionLat(), intersection.getIsDamaged());
//            results.add(a);
//        }
//        return results;
//    }
}
